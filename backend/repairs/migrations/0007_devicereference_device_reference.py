# Generated by Django 6.0.4 on 2026-06-10 03:04

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('repairs', '0006_device_model_number'),
    ]

    operations = [
        migrations.CreateModel(
            name='DeviceReference',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('category', models.CharField(choices=[('monitor', 'Monitor'), ('console', 'Console'), ('controller', 'Controller'), ('laptop', 'Laptop')], max_length=20)),
                ('brand', models.CharField(help_text='Manufacturer, e.g. Microsoft, Sony, ASUS.', max_length=120)),
                ('name', models.CharField(help_text="Model name, e.g. 'Xbox One S', 'DualSense', 'VG27AQ'.", max_length=160)),
                ('model_numbers', models.CharField(blank=True, help_text="Comma-separated label/part numbers off the unit (e.g. '1708'). Often blank until seen on a real unit — the search box matches name + brand too.", max_length=255)),
                ('release_year', models.PositiveSmallIntegerField(blank=True, null=True)),
                ('configurations', models.TextField(blank=True, help_text='Common variants — storage tiers, disc vs digital, board revisions.')),
                ('notes', models.TextField(blank=True, help_text='Signature fault / fix-vs-avoid one-liner.')),
            ],
            options={
                'verbose_name': 'device reference',
                'verbose_name_plural': 'device references',
                'ordering': ['category', 'brand', 'release_year', 'name'],
                'constraints': [models.UniqueConstraint(fields=('brand', 'name'), name='unique_reference_brand_name')],
            },
        ),
        migrations.AddField(
            model_name='device',
            name='reference',
            field=models.ForeignKey(blank=True, help_text='Optional link to the catalog entry for this model (year, configs, faults).', null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='units', to='repairs.devicereference'),
        ),
    ]
