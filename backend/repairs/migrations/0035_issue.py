# Generated by Django 6.0.4 on 2026-07-23 03:25

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('repairs', '0034_backfill_exits_from_markers'),
    ]

    operations = [
        migrations.CreateModel(
            name='Issue',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('verdict', models.CharField(choices=[('buy', 'Buy — in-lane fault'), ('avoid', 'Avoid — walk away'), ('caution', 'Caution — read the note')], max_length=10)),
                ('title', models.CharField(help_text="Chip text — short symptom/fault handle ('HDMI port dead', 'APU BGA').", max_length=120)),
                ('note', models.TextField(blank=True, help_text="The detail behind the verdict ('reflow is temporary — walk away as a flip').")),
                ('position', models.PositiveIntegerField(default=0, help_text="Manual ordering within the reference's list.")),
                ('reference', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='issues', to='repairs.devicereference')),
            ],
            options={
                'ordering': ['position', 'id'],
            },
        ),
    ]
